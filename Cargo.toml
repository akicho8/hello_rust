[package]
name = "hello_rust"
version = "0.1.0"
edition = "2021"
default-run = "hello_rust" # src/bin/* に他のファイルがある場合はこれが必要

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
